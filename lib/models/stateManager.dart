import 'package:flutter/material.dart';

class BottomStateManager {
  int bottomNavBarIndex;

  BottomStateManager({
    required this.bottomNavBarIndex,
  });
}

class TabBarStateManager {
   int tabIndex;

  TabBarStateManager({
    required this.tabIndex,
  });
}
